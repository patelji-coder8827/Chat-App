# Dependencies - These should be re-installed on the server (Render)
/backend/node_modules
/chat/node_modules

# Build Output - These should be generated by the deployment service
/chat/build
/chat/dist

# Sensitive Files - NEVER commit secrets
.env